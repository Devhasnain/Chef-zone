import { StyleSheet } from "react-native";


const Styles = StyleSheet.create({
    container: {
        position: 'relative',
        zIndex: 10,
        borderRadius: 15,
        marginVertical: 10,
    }
})

export default Styles;