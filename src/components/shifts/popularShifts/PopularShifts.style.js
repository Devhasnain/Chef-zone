import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container:{display:"flex",flexDirection:"column",gap:15},
  dotsContainer: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
  dot: {borderRadius: 50, marginHorizontal: 4},
});

export default styles;
