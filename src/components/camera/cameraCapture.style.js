import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    padding: 20,
  },
  preview: {
    width: '100%',
    height: 300,
    marginTop: 20,
  },
});

export default styles;
